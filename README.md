# Physics-Informed Quantum Reservoir Transformer for Bearing SHM

**Status**: Initial setup  
**Goal**: incipient bearing instability early warning

## Setup (Windows)
```powershell
python -m venv .venv
.venv\Scripts\activate
pip install -r requirements.txt